TAKEOVERS CHIEF OF STAFF
Phase 1
PWA Shell + Capture
Document 04 of 13
Estimated build time: 2-3 weeks
Outcome: Selmen captures every thought through voice or text. Nothing is lost.

Phase Goal
Phase 1 delivers the smallest possible system that produces real value: continuous capture. Selmen can voice-memo or text the agent at any time, the agent transcribes and structures the input, stores it, and surfaces a daily brief. Nothing else exists yet — no dispatch, no follow-up, no email handling, no team communication.
This phase is the foundation. By the end of it, Selmen has replaced his "never write down the to-do list" habit with a system that captures everything. That alone solves a meaningful portion of the day-to-day pain — and gives him a working surface to iterate on before adding more powerful (and riskier) capabilities.
What you can do at the end of Phase 1:
	•	Open the PWA on phone or desktop, log in
	•	Tap a voice button, speak a brain-dump, see it transcribed and structured
	•	Text the agent's Twilio number — same flow
	•	View the Today tab — daily brief, prioritized to-do list
	•	Tap any captured item to expand, edit, or mark done
	•	Receive a morning and evening brief automatically
What is explicitly NOT in this phase:
	•	No dispatch to team members (Phase 2)
	•	No follow-up scheduler (Phase 4)
	•	No email integration (Phase 3)
	•	No projects, insights, file access (later phases)

1. What to Build
1.1 The PWA Shell
A React + Vite PWA with the seven-tab navigation laid out. Only Tab 1 (Talk) and Tab 2 (Today) are functional in Phase 1. The other five tabs exist as empty placeholders — this avoids restructuring the navigation later.
Tabs and their Phase 1 status
Tab
Phase 1 status
Notes
Talk
FULLY FUNCTIONAL
Voice + text input, continuous thread
Today
FULLY FUNCTIONAL
Daily brief, captured tasks list
Approvals
Placeholder
Empty state: "Nothing pending"
Inbox
Placeholder
Empty state: "Email integration coming in Phase 3"
Team
READ-ONLY
Display-only — shows seeded team members
Projects
Placeholder
Empty state
Insights
Placeholder
Empty state
1.2 Authentication
Set up Clerk (or Auth0) as documented in the Build Environment Setup Guide. Phase 1 is single-user — only Selmen logs in. 2FA required. Session persistence across the PWA (so the user stays logged in for 30 days).
1.3 Talk Tab — The Continuous Thread
UI requirements
	•	Full-screen thread view, mobile-first, scrollable
	•	Bottom of screen: a sticky input bar with three controls — voice record button (large, primary), text input field, send button
	•	Voice button: tap-and-hold to record, release to send (iOS/Android pattern). Shows audio waveform while recording.
	•	Messages from Selmen appear right-aligned, agent responses left-aligned (standard chat pattern)
	•	Each message shows timestamp on tap
	•	Voice messages show as a play button with duration; below it, the transcribed text
	•	Agent's response includes the structured output: "I logged this as a task: [title]. Due: [proposed date]. Tap to edit."
	•	Pull to refresh for latest
	•	Thread persists — closing the app and reopening returns to the same scroll position
Voice flow
	•	User taps and holds the voice button
	•	Browser MediaRecorder API captures audio (webm/opus format)
	•	On release, audio is uploaded to the API
	•	API stores the file in S3 (or Replit storage), creates a message row with content_type='voice' and the storage URL
	•	API calls Whisper to transcribe; updates the message with content_text and transcription_confidence
	•	API calls Claude to parse the transcribed text (intent classification, entity extraction)
	•	API stores Claude's structured output in claude_parse JSONB column
	•	API creates downstream artifacts (task row, reminder, etc.) based on parse
	•	API responds to PWA with the structured result
	•	PWA renders the user's voice message + transcription + agent's structured response
Text flow
Same as voice but skip Whisper. Goes straight to Claude parse.
1.4 Today Tab — The Brief
Top section: Daily Brief
A card at the top shows the current daily brief. Updated automatically at 7 AM and 6 PM. Format:
GOOD MORNING, SELMEN
Thursday, May 1, 2026

ON YOUR PLATE TODAY
  • [3 highest-priority items, briefly]

OVERDUE
  • [items past due, if any]

CAPTURED OVERNIGHT
  • [count + summary of what came in since last brief]

AGENT NOTES
  • [any patterns the agent noticed worth surfacing]
Briefs are generated by Claude Sonnet, given the prior 12-24 hours of activity. Stored in a briefings table (or as a special row in messages with sender_type='agent', content_type='system').
Below the brief: Today's List
A prioritized list of tasks owned by Selmen (owner_id IS NULL). Each row shows:
	•	Priority badge (urgent / high / normal / low) on the left
	•	Task title
	•	Due date if set
	•	Capture timestamp ("captured 2 hours ago")
	•	Tap to expand: full description, edit button, mark complete button, delete button
Below the list: Recent Captures
Stream of the last 10-20 captured items, including reminders, decisions, and notes (not just tasks). Helps Selmen scan what he's been brain-dumping recently.
1.5 Capture parsing logic (Claude Sonnet prompt)
Every capture (voice or text) goes through a Claude call that classifies the input into one of seven types and extracts structured fields. The prompt template:
SYSTEM:
You are the parsing layer of Selmen's Chief of Staff agent. Your job is to take
a captured input and return structured JSON describing what it is and what
action should be taken.

Selmen is the CEO of Takeovers Rentals. He brain-dumps thoughts throughout
the day — tasks, reminders, decisions, ideas, questions. You classify each.

Return ONLY a JSON object with this shape:
{
  "type": "task" | "reminder" | "decision" | "context" | "question" | "draft_request" | "project",
  "title": "short summary, max 100 chars",
  "description": "full structured description",
  "proposed_owner_hint": null | "team member name if mentioned",
  "proposed_priority": "urgent" | "high" | "normal" | "low",
  "proposed_due": null | ISO timestamp,
  "tags": ["array", "of", "tags"],
  "requires_clarification": boolean,
  "clarification_question": null | "question to ask Selmen"
}

USER:
Captured input: "{transcribed_text}"

Recent context (last 5 captures):
{recent_captures_json}
In Phase 1, the agent does NOT yet act on proposed_owner_hint — that's Phase 2's job. The hint is captured in the database for later use.
1.6 SMS-to-Agent (Twilio)
In parallel to the PWA, Selmen can text the agent's dedicated Twilio number. Setup:
	•	Provision a Twilio number for the agent (production-only Twilio number; staging uses its own)
	•	Configure the Twilio number's webhook to POST to /api/webhooks/twilio/sms-inbound
	•	Webhook handler validates the Twilio signature, parses the inbound message, and routes it through the same capture pipeline as PWA input
	•	If the message is MMS containing audio, download the media URL, store, transcribe via Whisper
	•	After parsing, the agent replies via SMS with the structured confirmation ("Logged: [task title]. View in app.")
Webhook handler skeleton
// apps/api/src/routes/webhooks.ts
router.post('/webhooks/twilio/sms-inbound',
  twilioSignatureMiddleware,
  async (req, res) => {
    const { From, Body, MediaUrl0, NumMedia } = req.body;

    // Verify From matches Selmen's phone (single-user system)
    if (From !== process.env.PRINCIPAL_PHONE) {
      return res.status(403).send('Unauthorized sender');
    }

    // Get or create the principal_talk thread
    const thread = await getOrCreatePrincipalThread();

    let contentText = Body;
    let contentUrl = null;
    let contentType = 'text';

    if (NumMedia > 0 && MediaUrl0) {
      // Voice memo via MMS
      contentUrl = await downloadAndStoreMedia(MediaUrl0);
      contentText = await transcribeWhisper(contentUrl);
      contentType = 'voice';
    }

    // Process through capture pipeline
    const result = await processCapture({
      threadId: thread.id,
      contentText,
      contentUrl,
      contentType,
      direction: 'inbound',
      senderType: 'principal'
    });

    // Reply via SMS with structured confirmation
    return res.type('text/xml').send(
      buildTwiMLResponse(result.confirmationMessage)
    );
  }
);

2. Tech Decisions for This Phase
PWA framework
	•	React + Vite + Tailwind + shadcn/ui (per Master Spec)
	•	PWA capability via vite-plugin-pwa — manifest, service worker, install prompt
	•	State management: TanStack Query for server state, Zustand for ephemeral UI state
	•	Router: React Router v6, single-page app
Backend
	•	Node.js + Express 
	•	Drizzle ORM for Postgres queries — typed end-to-end
	•	BullMQ for background jobs (briefing generation, Whisper transcription queue)
	•	Zod for input validation on all API endpoints
Voice handling
	•	Browser: MediaRecorder API, captures webm/opus
	•	Storage: S3-compatible bucket (Replit Object Storage or AWS S3)
	•	Transcription: OpenAI Whisper API, model whisper-1
	•	Max recording length: 10 minutes (Whisper handles up to 25MB)
Claude integration
	•	Model for parse: claude-sonnet-4-6 (cheap, fast, sufficient for classification)
	•	Model for daily brief: claude-sonnet-4-6
	•	Max input tokens per parse: 8K (transcription + recent context)
	•	Streaming: Not needed in Phase 1 (responses are short)

3. API Endpoints to Build
3.1 Capture endpoints
POST   /api/capture                  // Submit a text capture
POST   /api/capture/voice            // Upload voice memo, returns task ID
GET    /api/capture/voice/:id        // Poll status (transcribing | parsed | done)
GET    /api/threads/principal        // Get principal_talk thread
GET    /api/threads/:id/messages     // Get messages in a thread, paginated
3.2 Today endpoints
GET    /api/today/brief              // Current daily brief
GET    /api/today/tasks              // Today's prioritized tasks (owner = principal)
GET    /api/today/recent-captures    // Recent capture stream
PATCH  /api/tasks/:id                // Update task (priority, due, status)
DELETE /api/tasks/:id                // Soft-delete task
3.3 Team endpoint (read-only Phase 1)
GET    /api/team                     // List active team members
3.4 Webhooks
POST   /api/webhooks/twilio/sms-inbound
POST   /api/webhooks/twilio/sms-status   // Delivery receipts
3.5 System
GET    /health
GET    /api/principal                // Selmen's profile
PATCH  /api/principal                // Update preferences

4. Background Jobs
4.1 Daily briefing job
	•	Schedule: 0 7 * * * (7 AM local) and 0 18 * * * (6 PM local)
	•	Action: Query last 12-24h of captures, recent task changes, send to Claude with the briefing prompt template, store result as a system message in the principal thread
	•	Push notification: Send web push (and SMS fallback if Selmen hasn't opened the PWA in 4+ hours)
4.2 Voice transcription queue
When a voice memo arrives via PWA or MMS, push a job to BullMQ. The worker downloads from storage, calls Whisper, updates the message row, then triggers Claude parse. Async processing prevents the user from waiting on the HTTP request.
4.3 Whisper retry logic
	•	If Whisper returns an error, retry up to 3 times with exponential backoff
	•	If still failing, mark message with transcription_confidence = 0 and a content_text of "[transcription failed - tap to retry]"
	•	Log to agent_actions_log with success=false

5. Acceptance Criteria
Phase 1 is done when all of the following are verified in staging against the shadow team and verifiable test data:
#
Acceptance criterion
How to verify
1
PWA loads on phone (iOS Safari) and desktop (Chrome) with all 7 tabs visible
Open both devices, log in, navigate every tab
2
Voice button records audio when held, releases to send
Test on iOS Safari and Android Chrome
3
Voice memos are transcribed within 30 seconds of upload
Send 10 voice memos, time each from upload to transcription appearing
4
Text input sends and gets parsed within 5 seconds
Send 10 text inputs, time each
5
Captures are correctly classified into the 7 types
Send 20 varied captures, manually verify the type field is correct on at least 18
6
Today tab loads daily brief at 7 AM and 6 PM local
Wait for both times, verify brief appears with correct data
7
Today tab list is sorted by priority then by due date
Create captures with mixed priorities, verify sort
8
SMS to the agent's Twilio number captures correctly
Text the staging Twilio number from your phone, verify it appears in the PWA thread
9
MMS voice memo (recorded in iMessage) is transcribed
Send a voice memo via SMS to the staging number, verify transcription
10
Push notification fires for the morning brief
Wait until 7 AM, verify push notification arrives
11
Continuous thread persists across sessions
Close the PWA, reopen 1 hour later, verify scroll position and full history present
12
Single-user auth blocks access from anyone except Selmen
Try to log in with another account, verify rejection
13
All capture artifacts are stored in the database with correct fields
Inspect Postgres after 50 test captures — verify messages, tasks, threads tables are populated correctly
14
Twilio webhook signature validation rejects forged requests
Send a request to /webhooks/twilio/sms-inbound without a valid signature, verify 403
15
Backend handles 100 captures in 5 minutes without errors
Run a load test sending 100 captures via API
16
Database has correct indexes — Today tab loads in under 500ms with 10k tasks seeded
Seed 10k test tasks, time the GET /api/today/tasks call

6. Testing Plan
6.1 Unit tests
	•	Capture parser: feed it 50 example transcriptions, assert classification correctness >= 90%
	•	Brief generator: feed it sample data sets, verify output structure
	•	Twilio signature validator: positive and negative cases
	•	Database operations: CRUD on every Phase 1 table
6.2 Integration tests
	•	End-to-end voice capture: upload → transcribe → parse → store → retrieve
	•	End-to-end SMS capture: webhook → parse → store → reply
	•	Daily brief generation against fixture data
	•	Auth flow: login → token refresh → session expiry
6.3 Manual QA
Run through this checklist in staging before merging to main:
	•	Send a voice memo from iPhone — verify everything works
	•	Send a voice memo from Android — verify
	•	Send a text capture — verify
	•	Send an SMS to the staging Twilio number — verify
	•	Wait for morning brief — verify
	•	Wait for evening brief — verify
	•	Mark a task complete — verify it disappears from Today tab
	•	Edit a task's priority — verify sort order updates
	•	Test offline: disable wifi mid-capture, verify graceful failure and retry
6.4 Adversarial tests
	•	Send a 9-minute voice memo — verify it transcribes correctly
	•	Send a captured input that's ambiguous ("figure out the thing") — verify Claude returns requires_clarification=true
	•	Send 20 captures in 30 seconds — verify queue handles burst
	•	Send a captured input in Spanish — verify transcription works (Whisper is multilingual)

7. Dependencies & Prerequisites
Before starting Phase 1
	•	Document 03 (Build Environment Setup) is complete — staging and production environments are deployed
	•	Database migrations for Phase 1 tables are written and applied (principal, team_members, threads, messages, tasks, agent_actions_log, schedules, feature_flags)
	•	Twilio account is set up, two numbers provisioned (one staging, one production)
	•	Anthropic API key is set up with separate keys per environment
	•	OpenAI API key is set up for Whisper
	•	S3 bucket(s) configured for voice storage
	•	Clerk (or Auth0) is configured for the PWA
Phase 1 has no upstream dependencies on other phases
Phase 1 is the foundation. Subsequent phases (Dispatch, Email, Chase, Projects, Insights, Files) all build on top of Phase 1's capture pipeline and database tables.

8. Risks & Mitigations
Risk: Whisper transcription is inaccurate for Selmen's accent or speaking style
Mitigation: Whisper supports an optional 'prompt' parameter that primes the model with vocabulary hints. Pass a prompt with team member names (Oscar, Nick, Josh, Orlando, Joan, Kathy, Karina, Tahir, Richard, Brandon), business terms (Takeovers, Fareharbor, ATV, UTV, sunflower), and locations (Miami, Dallas, Houston). This dramatically improves accuracy.
Risk: Claude misclassifies captures, leading to bad task suggestions
Mitigation: Phase 1 is read-only with respect to actions — the agent only stores and surfaces, it doesn't act. Misclassification just means a task is mis-tagged in the database. Selmen can edit any field. Override events feed the learning loop in Phase 6.
Risk: Voice memo upload fails on poor cell connection
Mitigation: Upload to S3 with retry; if fails, store the audio in browser IndexedDB and retry on next foreground. Show a 'queued' status in the thread.
Risk: PWA install prompt doesn't show on iOS
Mitigation: iOS doesn't auto-prompt PWA install. Add a manual instruction screen on first visit explaining how to add to Home Screen. This is unfortunate but unavoidable.
Risk: Twilio webhook latency causes user to send a duplicate
Mitigation: Inbound SMS handler should respond with TwiML in under 5 seconds. Move heavy work (Whisper transcription, Claude parse) to BullMQ queue, not the webhook synchronous path.
Risk: Daily brief is generic or low-quality
Mitigation: The first week of briefs will be rough because the agent has minimal context. This is expected. By week 2-3, with more captures in history, briefs become substantive. Don't tune the prompt aggressively before that — let real data accumulate first.

9. Phase 1 Sign-Off Checklist
When all of these are checked, Phase 1 is complete and you can move to Phase 2.
	•	All acceptance criteria in Section 5 verified in staging
	•	All Section 6 manual QA steps passed
	•	Database backup script is running daily and verified
	•	Sentry is capturing errors from staging
	•	Production environment is provisioned but no flags enabled yet
	•	Documentation in /docs is up to date with any deviations from this spec
	•	Selmen has used the staging system for at least 5 consecutive days without major issues
Once you've used the staging Phase 1 system for a week and you trust the capture loop, you're ready to go live in production with Phase 1 only — talk and today tabs, no agent actions toward team. This gives you immediate value while Phase 2 is being built. The momentum matters.
